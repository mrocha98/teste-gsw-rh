import React from 'react';
import NotFoundPage from '../NotFound/NotFound.page';

function Categorias() {
  return (
    <div
      style={{
        display: 'grid',
        placeItems: 'center',
        width: '100%',
        height: '100%',
      }}
    >
      <NotFoundPage />
    </div>
  );
}

export default Categorias;
